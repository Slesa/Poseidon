/*
using System.Globalization;
using Pms.Model.Entities;

namespace Pms.Model
{
    public class SumFormatter : IFormatSums
    {
        public string Format(decimal sum, Currency currency)
        {
            if (currency == null)
                return sum.ToString("C", CultureInfo.CurrentCulture);

            return null;
        }
    }
}*/