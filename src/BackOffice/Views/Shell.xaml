<Window
    x:Class="Poseidon.BackOffice.Views.Shell"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:Resources="clr-namespace:Poseidon.BackOffice.Resources"
    xmlns:DesignTime="clr-namespace:Poseidon.BackOffice.DesignTime"
    Title="{x:Static Resources:Strings.AppTitle}"
    Width="800"
    Height="600" 
    Style="{DynamicResource Style.Shell}"
    
    >
    

    <Window.Resources>
        <DesignTime:DesignTimeShellViewModel x:Key="DesignViewModel" />
    </Window.Resources>

    <Window.DataContext>DesignViewModel</Window.DataContext>
    
    <Window.InputBindings>
        <KeyBinding Key="X" Modifiers="Control" Command="{Binding OnQuitCommand}"/>
    </Window.InputBindings>

    <Grid>
        
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="1" Orientation="Horizontal">
                <Button Content="Back" Command ="{Binding OnBackCommand}"/>
                <TextBox x:Name="_breadCrumbBox" MinWidth="40"/>
                <TextBox x:Name="_searchBox"/>
        </StackPanel>
        
        <Grid/>
    </Grid>

</Window>
