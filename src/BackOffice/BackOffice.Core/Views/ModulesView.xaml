<UserControl x:Class="Poseidon.BackOffice.Core.Views.ModulesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:DesignTime="clr-namespace:Poseidon.BackOffice.Core.DesignTime"
             xmlns:core="clr-namespace:Poseidon.BackOffice.Core"
             xmlns:local="clr-namespace:Poseidon.BackOffice.Core.ViewModels"
             xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity"
             xmlns:cal="clr-namespace:Caliburn.Micro;assembly=Caliburn.Micro"
             xmlns:common="clr-namespace:Poseidon.BackOffice.Common;assembly=Poseidon.BackOffice.Common"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    
    <UserControl.Resources>
        <core:ModulesViewModelLocator x:Key="ModulesViewModel" />

        <HierarchicalDataTemplate DataType="{x:Type local:ModuleViewModel}">
            <Border ToolTip="{Binding ToolTip}" HorizontalAlignment="Left">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="Click">
                        <cal:ActionMessage MethodName="{Binding Activate}" />
                    </i:EventTrigger>
                </i:Interaction.Triggers>

                <StackPanel Orientation="Vertical">

                    <Grid Background="LightBlue" FocusVisualStyle="{x:Null}" Margin="10">
                       <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <Image Grid.RowSpan="2" Width="32" Height="32" Source="{Binding IconFileName}"/>
                        <TextBlock Grid.Column="1" Text="{Binding Title}" FontWeight="Bold" />
                        <TextBlock Grid.Column="1" Grid.Row="1" Text="{Binding Description}"/>
                    </Grid>
                    <ItemsControl ItemsSource="{Binding Children}" />
                </StackPanel>
                <!--
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="Click">
                        <i:InvokeCommandAction Command="{Binding SelectedCommand}"/>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
-->
            </Border>
        </HierarchicalDataTemplate>

        <HierarchicalDataTemplate DataType="{x:Type common:IOfficeModule}">
            <Border ToolTip="{Binding ToolTip}" HorizontalAlignment="Stretch">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="Click">
                        <cal:ActionMessage MethodName="{Binding Activate}" />
                    </i:EventTrigger>
                </i:Interaction.Triggers>

                <StackPanel Orientation="Vertical">

                    <Grid Background="LightBlue" FocusVisualStyle="{x:Null}">
                       <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <Image Grid.RowSpan="2" Width="32" Height="32" Source="{Binding IconFileName}"/>
                        <TextBlock Grid.Column="1" Text="{Binding Title}" FontWeight="Bold" />
                        <TextBlock Grid.Column="1" Grid.Row="1" Text="{Binding Description}"/>
                    </Grid>
                    <ItemsControl ItemsSource="{Binding Children}" />
                </StackPanel>
                <!--
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="Click">
                        <i:InvokeCommandAction Command="{Binding SelectedCommand}"/>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
-->
            </Border>
        </HierarchicalDataTemplate>

    </UserControl.Resources>

    <UserControl.DataContext>
        <Binding Path="ViewModel" Source="{StaticResource ModulesViewModel}" />
    </UserControl.DataContext>
    <d:UserControl.DataContext>
        <DesignTime:DesignTimeModulesViewModel />
    </d:UserControl.DataContext>

    <Grid>
         <TreeView ItemsSource="{Binding Modules}" />   
    </Grid>
</UserControl>
