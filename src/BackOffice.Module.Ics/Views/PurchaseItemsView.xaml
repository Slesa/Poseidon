<UserControl x:Class="Poseidon.BackOffice.Module.Ics.Views.PurchaseItemsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:ics="clr-namespace:Poseidon.BackOffice.Module.Ics"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.Resources>
        <ics:PurchaseItemsViewModelLocator x:Key="PurchaseItemsViewModel" />

        <DataTemplate x:Key="ItemTemplate.PurchaseItem">
            <StackPanel Orientation="Horizontal">
                <TextBlock VerticalAlignment="Center" Text="{Binding ID}"/>
                <TextBlock VerticalAlignment="Center" Text="{Binding Name}"/>
            </StackPanel>
        </DataTemplate>
    </UserControl.Resources>

    <UserControl.DataContext>
        <Binding Path="ViewModel" Source="{StaticResource PurchaseItemsViewModel}" />
    </UserControl.DataContext>

    <DockPanel LastChildFill="True">
        <Border DockPanel.Dock="Top" BorderBrush="BlueViolet" BorderThickness="2">
            <TextBox Text="Purchase items" TextAlignment="Center" />
        </Border>
        <ListBox ItemsSource="{Binding PurchaseItems}" ItemTemplate="{StaticResource ItemTemplate.PurchaseItem}" />
    </DockPanel>
</UserControl>
